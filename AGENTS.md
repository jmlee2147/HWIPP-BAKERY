## HWIPP BAKERY 에이전트 지침

이 파일은 AI 코딩 에이전트의 진입점입니다. 프로젝트 공통 규칙과 문서 위치를 안내합니다.

### 사고 방식

- 사용자가 다르게 요청하지 않으면 한국어로 사고하고 응답합니다.
- 코드 식별자(변수/함수/파일명), 터미널 명령어, 라이브러리·패키지 고유명사를 제외하고 영어 문장을 한국어 응답에 섞지 않습니다.
- 구현 전에 관련 파일과 지침을 먼저 확인합니다.
- 작업은 작고 검증 가능한 단위로 나눕니다.
- `pnpm`만 사용하고 `npm`/`yarn`은 사용하지 않습니다.
- 기존 사용자 변경을 보존하고 관련 없는 작업을 되돌리지 않습니다.
- 검증 결과와 검증하지 못한 항목을 최종 보고에 명시합니다.
- **성능·메모리 주장은 수치로 증명합니다.** "부드럽게 동작합니다"는 근거가 아닙니다.

### 이 프로젝트에서 판단이 갈리는 지점

일반적인 웹 개발 상식이 여기서는 틀리는 경우가 많습니다. 확신이 서기 전에 규칙을 먼저 읽습니다.

| 흔한 판단 | 이 프로젝트 |
| --- | --- |
| 반응형으로 만든다 | **고정 캔버스 + 전체 scale.** 표시 기기가 하나뿐입니다 |
| `next/image`로 최적화 | **정적 `<img>` + 프리로드.** 로컬 고정 에셋이라 런타임 최적화는 손해입니다 |
| 파일 용량으로 에셋 예산을 잡는다 | **디코딩 메모리** 기준입니다. 풀스크린 1장 ≈ 14MB RAM |
| hover로 어포던스를 준다 | **터치 전용이라 hover가 없습니다** |
| 에러는 사용자에게 알린다 | **관람객에게 에러를 보여주지 않습니다.** 폴백 또는 `idle` 복귀 |
| 문제가 생기면 운영자가 조치한다 | **절차로 넘기지 말고 설계로 풉니다** |

### 규칙 로딩

#### Tier 1: 코드 변경 시 항상 로드

| 파일 | 목적 |
| --- | --- |
| `.claude/rules/working-rules.md` | 코드 품질 원칙 · 작업 흐름 · 검증 기준 |
| `.claude/rules/project-constraints.md` | 프로젝트 금지/제약 조건 |

단순 질문이나 설명 요청에서는 이 `AGENTS.md`만으로 답합니다.

#### Tier 2: 작업에 필요한 경우에만 로드

| 작업 | 로드할 문서 |
| --- | --- |
| 파일 배치, import, 레이어 경계, 고정 캔버스, 코드 스타일, 데이터 패칭 | `.claude/rules/architecture.md` |
| 재사용 UI 생성 또는 수정 | `.claude/rules/component-guide.md` |
| 세션 상태, STEP 전이, 수정하기 재진입, 유휴 타임아웃 | `.claude/rules/state-machine.md` |
| 화면 전환, Lottie, 인터랙션 피드백 | `.claude/rules/motion.md` |
| 이미지 변환, 매니페스트, 프리캐싱, 메모리 예산 | `.claude/rules/assets.md` |
| Claude API, 결과 스키마, 폴백, 공개 API 보호 | `.claude/rules/ai-pipeline.md` |
| 자가 복구, 모니터링, webOS 설정, 출시 기준 | `.claude/rules/operations.md` |
| Figma 디자인을 컴포넌트로 옮기기 | `.claude/rules/architecture.md`, `.claude/rules/motion.md` |
| 테스트 추가 또는 수정 | `.claude/rules/testing.md` |
| PR/커밋 문구 작성, 브랜치 이름 | `.claude/rules/commit-convention.md` |

### Skills

| Skill | 용도 |
| --- | --- |
| `figma-to-component` | Figma 노드를 컴포넌트로 옮기기 |
| `asset-cleanup` | Figma 에셋 중복 제거·병합 |
| `code-review` | 변경 코드 리뷰 (읽기 전용) |
| `commit-kr` | 한국어 커밋 메시지 제안 |
| `create-pr` | PR 제목·본문 초안 |

Skill 파일은 `.claude/skills/<skill-name>/SKILL.md` 에 있습니다.

### 참고 문서

| 문서 | 내용 |
| --- | --- |
| `docs/ARCHITECTURE.md` | 설계 결정과 근거, 검토했으나 버린 대안, 미확정 사항 |
| `docs/decisions/` | 개별 결정 기록 (ADR) |

### 금지 패턴

- **요청받기 전에 커밋하지 않습니다.** 작업 요청은 커밋 요청이 아닙니다.
- **GitHub 이슈·마일스톤을 임의로 만들지 않습니다.** 사용자가 직접 관리합니다.
- **이모지를 쓰지 않습니다.** 주석·문서·커밋 메시지 전부. 화면에 렌더링되는 디자인 요소만 예외입니다.
- **주석에 배경을 늘어놓지 않습니다.** 결론만 쓰고 근거는 규칙 문서 경로로 가리킵니다.
- 모든 규칙 파일을 기본으로 로드하지 않습니다. Tier 기준을 따릅니다.
- `scale(4/3)`처럼 배율을 하드코딩하지 않습니다. `getCanvasScale()`을 씁니다.
- `cursor: none`을 지우지 않습니다. 터치 전용이므로 커서는 숨기는 것이 맞습니다.
- hover를 유일한 어포던스로 쓰지 않습니다.
- Tailwind v4, `:has()`, container query, CSS nesting을 쓰지 않습니다.
- 관리자 대시보드를 만들지 않습니다.
- 운영 절차로 해결하자는 결론을 내지 않습니다. 자가 복구로 풉니다.
